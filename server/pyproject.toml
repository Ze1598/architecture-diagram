[project]
name = "archd-server"
version = "0.1.0"
description = "MCP server + HTTP bridge for the architecture diagram editor"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.27.0",
    "pydantic>=2.0.0",
]
